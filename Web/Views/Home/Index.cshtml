@model Web.Models.HomeControllerIndexModel
@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div class="col-md-6">
        <h2>Onlinestatus</h2>
        <table class="table table-hover table-responsive table-striped">
            <thead>
            <tr>
                <th>Gerät</th>
                <th>Onlinestatus</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var ping in Model.Pings)
            {
                <tr>
                    <td>@ping.Hostname</td>
                    <td>@if (ping.ConsideredOnline)
                        {
                            <span class="label label-success">online</span>
                        }
                        else
                        {
                            <span class="label label-danger">offline</span>
                        }</td>
                    <td>@if (ping.ConsideredOnline)
                        {
                            @Html.ActionLink("Licht an", "SetLight", new {DeviceName=ping.Hostname,DesiredState=true}, new {@class="btn btn-success"})
                            @Html.ActionLink("Licht aus", "SetLight", new {DeviceName=ping.Hostname,DesiredState=false}, new {@class="btn btn-danger"})
                        }</td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
